<h1>Burgers!</h1>

<h2>Burgers that are not gooey!</h2>

<ul>
  {{#each burgers}}
    {{#unless gooey}}
      {{>burgers/burger-block gooey=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are gooey!</h2>

<ul>
  {{#each burgers}}
    {{#if gooey}}
      {{> burgers/burger-block gooey=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">
  <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="gooey">Gooey?</label><br>
    <input type="radio" name="gooey" value="1" checked> Gooey!<br>
    <input type="radio" name="gooey" value="0"> Dry
  </div>

  <button type="submit">Add Burger</button>

</form>